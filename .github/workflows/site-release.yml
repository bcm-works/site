#
#
# GitHub Actions Workflow: Site Release
#   - This workflow includes the build, test, deploy, and release processes
#   - Initial setup is required, refer to: src/site/README.md > Infrastructure
#
#

name: Site Release
run-name: Site Release by ${{ github.actor }} on ${{ github.ref_name }}

on:
  # Manual run via GitHub Repo > Actions > Site Release > Run workflow
  workflow_dispatch:
    inputs:
      continue_deployment:
        description: 'Confirm production deployment and release for Public Website'
        type: boolean
        required: true
        default: false

defaults:
  run:
    working-directory: src/site
    shell: bash

permissions:
  contents: read

jobs:

  build:
    name: Build
    if: ${{ inputs.continue_deployment == true }}
    runs-on: ubuntu-latest
    environment:
      name: ci-build
      deployment: false
    env: ${{ secrets }}
    outputs:
      TIMESTAMP: ${{ steps.vars.outputs.TIMESTAMP }}
      COMMIT: ${{ steps.vars.outputs.COMMIT }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 1
          submodules: recursive
      - name: Set workflow variables
        id: vars
        run: |
          echo "TIMESTAMP=$(TZ='Australia/Sydney' date +%Y%m%d.%H%M)" >> $GITHUB_OUTPUT
          echo "COMMIT=$(git rev-parse --short ${{ github.sha }})" >> $GITHUB_OUTPUT
      - name: Deno setup
        uses: denoland/setup-deno@v2
        with:
          deno-version: latest
      - name: Install dependencies
        run: deno ci --quiet
      - name: Build site
        run: deno task build
      - name: Upload build files
        uses: actions/upload-artifact@v7
        with:
          name: build-${{ steps.vars.outputs.COMMIT }}
          overwrite: true
          include-hidden-files: true
          retention-days: 1
          compression-level: 9
          path: |
            src/site/public
            src/site/.env
  test:
    name: Test
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: ci-build
      deployment: false
    env: ${{ secrets }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 1
          submodules: recursive
      - name: Download build files
        uses: actions/download-artifact@v8
        with:
          name: build-${{ needs.build.outputs.COMMIT }}
          path: ${{ github.workspace }}/src/site
      - name: Deno setup
        uses: denoland/setup-deno@v2
        with:
          deno-version: latest
      - name: Run tests
        run: deno task test

  push-docker-hub:
    name: Push to Docker Hub
    needs: [build, test]
    runs-on: ubuntu-latest
    environment:
      name: ci-build
      deployment: false
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 1
          submodules: recursive
      - name: Docker Hub login
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Setup QEMU
        uses: docker/setup-qemu-action@v4
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build and push Docker Image
        id: push
        uses: docker/build-push-action@v7
        with:
          context: ${{ github.workspace }}/src/site
          file: ${{ github.workspace }}/src/site/Dockerfile
          platforms: linux/arm64
          provenance: false
          push: true
          build-args: |
            SITE_FEED_TITLE=${{ secrets.SITE_FEED_TITLE }}
            SITE_FEED_DESC=${{ secrets.SITE_FEED_DESC }}
            SITE_FEED_DEFAULT_TITLE=${{ secrets.SITE_FEED_DEFAULT_TITLE }}
            SITE_LANG=${{ secrets.SITE_LANG }}
            SITE_AUTHOR=${{ secrets.SITE_AUTHOR }}
            SITE_URL=${{ secrets.SITE_URL }}
            SITE_POSTHOG_ID=${{ secrets.SITE_POSTHOG_ID }}
            SITE_POSTHOG_API_HOST=${{ secrets.SITE_POSTHOG_API_HOST }}
            SITE_POSTHOG_UI_HOST=${{ secrets.SITE_POSTHOG_UI_HOST }}
          tags: |
            docker.io/${{ secrets.SITE_DOCKER_IMAGE_PATH }}:latest
            docker.io/${{ secrets.SITE_DOCKER_IMAGE_PATH }}:branch-${{ github.ref_name }}
            docker.io/${{ secrets.SITE_DOCKER_IMAGE_PATH }}:commit-${{ needs.build.outputs.COMMIT }}
            docker.io/${{ secrets.SITE_DOCKER_IMAGE_PATH }}:release-${{ needs.build.outputs.TIMESTAMP }}
          labels: |
            maintainer=${{ secrets.SITE_AUTHOR }}
            org.opencontainers.image.licenses=MIT
            org.opencontainers.image.title=${{ secrets.SITE_TITLE }}
            org.opencontainers.image.description=${{ secrets.SITE_DESC }}
            org.opencontainers.image.authors=${{ secrets.SITE_AUTHOR }}
            org.opencontainers.image.vendor=${{ secrets.SITE_AUTHOR }}
            org.opencontainers.image.url=${{ secrets.SITE_REPO }}
            org.opencontainers.image.source=${{ secrets.SITE_REPO }}
            org.opencontainers.image.version=${{ needs.build.outputs.TIMESTAMP }}
            org.opencontainers.image.revision=${{ needs.build.outputs.COMMIT }}

  deploy-gcp-cloud-run:
    name: Deploy to GCP
    needs: [build, test, push-docker-hub]
    runs-on: ubuntu-latest
    environment:
      name: gcp-cloud-run
      deployment: false
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 1
          submodules: recursive
      - name: Google Cloud login
        id: auth
        uses: google-github-actions/auth@v3
        with:
          project_id: ${{ secrets.SITE_GCP_PROJECT_ID }}
          credentials_json: ${{ secrets.SITE_GCP_SERVICE_ACCOUNT_JSON }}
      - name: Cloud Run deployment
        uses: google-github-actions/deploy-cloudrun@v3
        with:
          service: ${{ secrets.SITE_GCP_CLOUD_RUN_SERVICE_NAME }}
          region: ${{ secrets.SITE_GCP_REGION }}
          image: ${{ secrets.SITE_GCP_DOCKER_IMAGE_URL }}/${{ secrets.SITE_DOCKER_IMAGE_PATH }}:release-${{ needs.build.outputs.TIMESTAMP }}

  release:
    name: Release
    needs: [build, test, push-docker-hub, deploy-gcp-cloud-run]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Create release tag
        run: |
          git config user.name "bot-release"
          git config user.email "brendan+bot-release@murty.au"
          git tag release-${{ needs.build.outputs.TIMESTAMP }}
          git push --tags --quiet
      - name: Generate release notes
        working-directory: ${{ github.workspace }}
        run: |
          chmod +x ${{ github.workspace }}/bin/release-notes.sh
          bash ${{ github.workspace }}/bin/release-notes.sh ${{ github.workspace }}/release-notes.log
      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          name: ${{ needs.build.outputs.TIMESTAMP }}
          tag: release-${{ needs.build.outputs.TIMESTAMP }}
          bodyFile: ${{ github.workspace }}/release-notes.log
          makeLatest: true
