# News

This directory contains a self-hosted version of the [Commafeed](https://github.com/Athou/commafeed/) RSS reader service.

## Local Environment

Start the local server:

```bash
./bin/news-start.sh
```

Stop the local server:

```bash
./bin/news-stop.sh
```
