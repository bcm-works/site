#
#
# Git: GitHub Secrets example file
#   - Initial manual setup required, follow steps in: src/git/README.md > Infrastructure
#   - Add a new Secret for each item below:
#     - Follow the navigation steps in each section to ensure it's added to the right area
#     - Set the name to the value before the '=' below, eg: GIT_APP_AUTHOR
#     - Set the value of each new Secret to suit your setup
#
#

#
# ----
# Repository Secrets
# ----
#
# Add these to:
#   GitHub Repo > Settings >  Security and qaulity > Secrets and variables >
#   Actions > Repository secrets
#

GIT_APP_NAME=bcm-git
GIT_APP_ENV=production

GIT_APP_SLOGAN=git service
GIT_APP_DISPLAY_NAME_FORMAT={APP_NAME}

GIT_APP_THEME_LIST=bcm,forgejo-auto,forgejo-light,forgejo-dark
GIT_APP_THEME_DEFAULT=bcm

GIT_URL=https://example.com/
GIT_PORT_WEB=3000
GIT_PORT_SSH=2222

#
# ----
# GitHub Environment: gcp-cloud-run
# ----
#
# Add these to:
#   GitHub Repo > Settings > Code and automation > Environments >
#   gcp-cloud-run > Add environment secret
#
# These credentials are used to deploy the Docker Image to GCP Cloud Run.
#

GIT_GCP_PROJECT_ID=jane-git-333444
GIT_GCP_REGION=asia-southeast1
GIT_GCP_CLOUD_RUN_SERVICE_NAME=jane-git-production
GIT_GCP_SERVICE_ACCOUNT_JSON={...}

GIT_DOCKER_IMAGE_URL_FORGEJO=asia-southeast1-docker.pkg.dev/xxxx1111/codeberg/forgejo/forgejo:15-rootless
