# Buildkite - Agent Image Dockerfile
# Apply security updates and install required packages.
RUN apt-get update && apt-get upgrade -y && apt-get install -y --no-install-recommends \
    bash git ca-certificates
